package constructor_in_Interface;

abstract class C {
	abstract void test(); //Incomplete method. 
		//If we use abstract keyword for methods then the whole class will become Abstract class.
}
//We get no output for this as it has no main method.